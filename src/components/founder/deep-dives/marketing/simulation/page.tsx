import { SimulationClient } from "./SimulationClient"

export const metadata = {
  title: "Marketing Simulation — Bloom & Co.",
  description: "An interactive simulation where you run marketing for a real Australian e-commerce brand from day one.",
}

export default function SimulationPage() {
  return <SimulationClient />
}
